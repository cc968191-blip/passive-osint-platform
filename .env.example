# Example environment variables for Passive OSINT Platform
# Copy this file to .env and fill values before deploying
# IMPORTANT: Generate strong random values for SECRET_KEY and API_TOKEN
#   python -c "import secrets; print(secrets.token_hex(32))"

# Flask
FLASK_ENV=production
FLASK_HOST=0.0.0.0
PORT=5000
SECRET_KEY=
API_TOKEN=

# CORS - comma-separated list of allowed origins (no wildcard in production)
CORS_ORIGINS=http://localhost:5000

# Optional API keys (leave empty if not used)
VIRUSTOTAL_API_KEY=
SHODAN_API_KEY=
SECURITYTRAILS_API_KEY=
CENSYS_API_ID=
CENSYS_API_SECRET=

# Logging
LOG_LEVEL=INFO
